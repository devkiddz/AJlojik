    # AJ Logik — MS11.07 Support Intelligence and Operations

    ## Dependency

    Apply only after **MS11.06 committed and pushed**.

    ## Install

    Extract this ZIP into the AJ Logik project root.

    ```powershell
    node .\apply.ms11.07-support-intelligence-operations.mjs
    Remove-Item .\apply.ms11.07-support-intelligence-operations.mjs
    ```

    ## Validate

    ```powershell
    npm run typecheck
npm run lint
npm run build

    git status --short
    git diff --stat
    ```

    Stop immediately if any command fails.

    ## Stage only this milestone

    ```powershell
    git add `
      features/support/supportIntelligenceTypes.ts `
  features/support/server/supportIntelligenceService.ts `
  features/support/server/supportOperationsDashboardRepository.ts `
  features/support/components/SupportIntelligencePanel.tsx `
  features/support/components/SupportOperationsDashboard.tsx `
  app/api/admin/support/cases/[caseId]/intelligence/route.ts `
  app/admin/support/operations/page.tsx `
  features/support/index.ts `
  features/support/components/AgentSupportCaseWorkspace.tsx `
  features/support/components/AgentSupportQueue.tsx

    git diff --cached --stat
    git status --short
    ```

    Confirm installers, ZIPs, backups and inspection files are not staged.

    ## Commit

    ```powershell
    git commit -m "feat(support): add operations intelligence assistance"
    git push
    ```

    Do not install the next milestone until this commit succeeds.
