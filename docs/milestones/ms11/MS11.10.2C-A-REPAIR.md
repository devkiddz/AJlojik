# AJ Logik — MS11.10.2C-A Quick Support Wiring Repair

The original MS11.10.2C installer created and replaced its first five files, but
stopped before wiring the feature into the live Quick Chat workspace.

This repair updates only:

- `features/support/components/QuickSupportChatWorkspace.tsx`
- `features/support/index.ts`
- `package.json`

It does not rewrite the five already-correct MS11.10.2C files and does not touch
the separate featured-experience work.

## Install

```powershell
node .\apply.ms11.10.2c-a-quick-support-wiring-repair.mjs
Remove-Item .\apply.ms11.10.2c-a-quick-support-wiring-repair.mjs

npm run verify:support-quick-history
npm run verify:support-quick-unread
npm run verify:support-quick-continuity
npm run verify:support-quick-chat
npm run verify:support-live
npm run typecheck
npm run lint
npm run build
```

## Expected new verifier

```text
AJ Logik Quick Support multi-case continuity contract is complete.
Validated 7 multi-case boundaries.
```

Do not stage until `git status --short` shows all eight MS11.10.2C files.
