    # AJ Logik — MS11.06 Support Operations Governance

    ## Dependency

    Apply only after **MS11.05 committed and pushed**.

    ## Install

    Extract this ZIP into the AJ Logik project root.

    ```powershell
    node .\apply.ms11.06-support-operations-governance.mjs
    Remove-Item .\apply.ms11.06-support-operations-governance.mjs
    ```

    ## Validate

    ```powershell
    npx prisma format
npx prisma validate
npx prisma generate
npx prisma migrate status
npx prisma migrate deploy
npm run typecheck
npm run lint
npm run build

    git status --short
    git diff --stat
    ```

    Stop immediately if any command fails.

    ## Stage only this milestone

    ```powershell
    git add `
      features/support/supportOperationsTypes.ts `
  features/support/server/supportOperationsRepository.ts `
  features/support/server/supportOperationsService.ts `
  features/support/components/SupportOperationsPanel.tsx `
  app/api/admin/support/cases/[caseId]/operations/route.ts `
  prisma/migrations/20260802103000_add_support_commerce_actions/migration.sql `
  prisma/schema.prisma `
  features/admin/auth/adminPermissions.ts `
  features/support/index.ts `
  features/support/components/AgentSupportCaseWorkspace.tsx `
  app/admin/support/[caseId]/page.tsx

    git diff --cached --stat
    git status --short
    ```

    Confirm installers, ZIPs, backups and inspection files are not staged.

    ## Commit

    ```powershell
    git commit -m "feat(support): add governed operations and commerce context"
    git push
    ```

    Do not install the next milestone until this commit succeeds.
