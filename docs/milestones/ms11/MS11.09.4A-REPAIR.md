# AJ Logik — MS11.09.4A Live Workspace Repair

This repair addresses the exact failures from the MS11.09.4 validation log.

## Corrected

- Customer Support workspace live subscription.
- Customer live status, presence and typing UI.
- Agent Support workspace live subscription.
- Agent live status, presence, typing and read-state UI.
- Admin `mark-read` Support action.
- Active-conversation notification suppression.
- React lint violation caused by updating `onEventRef.current` during render.

## Preserved

- 32 existing migrations.
- Database-backed SSE transport.
- Presence schema.
- Production diagnostics and retention.
- Existing Support case actions and permissions.
- Existing UI structure outside the live additions.

## Install

```powershell
node .\apply.ms11.09.4a-live-workspace-repair.mjs
Remove-Item .\apply.ms11.09.4a-live-workspace-repair.mjs

npm run verify:support-live
npm run typecheck
npm run lint
npm run build
```

No Prisma command, migration, or dependency installation is required.

Expected verification:

```text
AJ Logik Support live runtime contract is complete.
Validated 14 runtime boundaries.
```

Existing unrelated lint warnings may remain, but the Support live runtime must
produce no lint error.
