import Link from 'next/link';
import { BadgePercent, CalendarClock, ImageIcon, Tags } from 'lucide-react';

import { getAdminAccess } from '@/features/admin/auth/adminPermissions';
import { AdminMetric, AdminPage, AdminPageHeader, AdminPanel, adminFieldClass } from '@/features/admin/components';
import { MediaChoiceGrid } from '@/features/admin/media';
import { savePromotion, setPromotionStatus } from '@/features/admin/promotions/actions';
import { prisma } from '@/lib/prisma';

function datetime(value: Date | null | undefined) { if (!value) return ''; const offset = value.getTimezoneOffset(); return new Date(value.getTime() - offset * 60_000).toISOString().slice(0, 16); }

export default async function AdminPromotionsPage({ searchParams }: { searchParams: Promise<{ edit?: string }> }) {
  const access = await getAdminAccess();
  if (!access.permissions.has('promotion:view')) throw new Error('Promotion Studio access is required.');
  const { edit } = await searchParams;
  const workspaceId = access.membership.workspaceId;
  const [promotions, products, media, vendors, editing] = await Promise.all([
    prisma.promotion.findMany({ where: { workspaceId, status: { not: 'ARCHIVED' } }, include: { bannerMediaAsset: true, vendorProfile: { select: { name: true } }, products: true }, orderBy: [{ priority: 'desc' }, { updatedAt: 'desc' }] }),
    prisma.product.findMany({ where: { workspaceId, status: 'PUBLISHED' }, orderBy: { name: 'asc' }, select: { id: true, name: true } }),
    prisma.mediaAsset.findMany({ where: { workspaceId, status: 'ACTIVE', resourceType: 'IMAGE' }, orderBy: { createdAt: 'desc' }, take: 150 }),
    access.membership.workspace.commerceMode === 'MULTI_VENDOR' ? prisma.vendorProfile.findMany({ where: { workspaceId, status: 'ACTIVE', active: true }, orderBy: { name: 'asc' }, select: { id: true, name: true } }) : Promise.resolve([]),
    edit ? prisma.promotion.findFirst({ where: { id: edit, workspaceId }, include: { products: true } }) : null
  ]);
  const active = promotions.filter(item => item.status === 'PUBLISHED' && item.active).length;
  const scheduled = promotions.filter(item => item.startsAt && item.startsAt > new Date()).length;
  return <AdminPage><div className="mx-auto max-w-[96rem] space-y-5"><AdminPageHeader eyebrow="Offer management" title="Promotion Studio" description="Create, edit and schedule product or vendor offers using reusable Media Studio galleries." /><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><AdminMetric icon={BadgePercent} label="Promotions" value={promotions.length} /><AdminMetric icon={Tags} label="Active" value={active} /><AdminMetric icon={CalendarClock} label="Scheduled" value={scheduled} /><AdminMetric icon={ImageIcon} label="Banner assets" value={media.length} /></section>
  {access.permissions.has('promotion:manage') ? <AdminPanel title={editing ? `Edit ${editing.title}` : 'Create promotion'} description="The public banner is selected visually from Media Studio."><form action={savePromotion} className="grid gap-4 lg:grid-cols-3">{editing ? <input type="hidden" name="id" value={editing.id} /> : null}<Field label="Title"><input name="title" defaultValue={editing?.title} required className={adminFieldClass} /></Field><Field label="Slug"><input name="slug" defaultValue={editing?.slug} className={adminFieldClass} /></Field><Field label="Type"><select name="type" defaultValue={editing?.type ?? 'PERCENTAGE'} className={adminFieldClass}><option value="PERCENTAGE">Percentage</option><option value="FIXED_AMOUNT">Fixed amount</option><option value="FIXED_PRICE">Fixed product price</option><option value="FEATURED">Featured campaign</option></select></Field><Field label="Discount value"><input name="discountValue" type="number" min="0" step="0.01" defaultValue={editing?.discountValue === null || editing?.discountValue === undefined ? '' : Number(editing.discountValue)} className={adminFieldClass} /></Field><Field label="Code"><input name="code" defaultValue={editing?.code ?? ''} className={adminFieldClass} /></Field><Field label="Usage limit"><input name="usageLimit" type="number" min="0" defaultValue={editing?.usageLimit ?? ''} className={adminFieldClass} /></Field><Field label="Starts"><input name="startsAt" type="datetime-local" defaultValue={datetime(editing?.startsAt)} className={adminFieldClass} /></Field><Field label="Ends"><input name="endsAt" type="datetime-local" defaultValue={datetime(editing?.endsAt)} className={adminFieldClass} /></Field><Field label="Priority"><input name="priority" type="number" defaultValue={editing?.priority ?? 0} className={adminFieldClass} /></Field>{vendors.length ? <Field label="Vendor owner"><select name="vendorProfileId" defaultValue={editing?.vendorProfileId ?? ''} className={adminFieldClass}><option value="">Workspace promotion</option>{vendors.map(vendor => <option key={vendor.id} value={vendor.id}>{vendor.name}</option>)}</select></Field> : null}<Field label="Status"><select name="status" defaultValue={editing?.status === 'REJECTED' ? 'DRAFT' : editing?.status ?? 'DRAFT'} className={adminFieldClass}><option value="DRAFT">Draft</option><option value="PENDING_REVIEW">Pending review</option>{access.permissions.has('approval:review') ? <><option value="PUBLISHED">Published</option><option value="PAUSED">Paused</option></> : null}</select></Field><label className="flex min-h-11 items-center gap-2 rounded-2xl border border-border/60 bg-background/60 px-4 text-xs font-bold"><input type="checkbox" name="active" defaultChecked={editing?.active ?? true} /> Active after publication</label><Field label="Description" className="lg:col-span-3"><textarea name="description" rows={3} defaultValue={editing?.description ?? ''} className={adminFieldClass} /></Field><fieldset className="lg:col-span-3"><legend className="mb-2 text-[9px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Banner gallery</legend><MediaChoiceGrid media={media} name="bannerMediaAssetId" initialIds={editing?.bannerMediaAssetId ? [editing.bannerMediaAssetId] : []} emptyLabel="No banner" purpose="promotions" uploadAccept="image" acceptedResourceTypes={['IMAGE']} /></fieldset><fieldset className="lg:col-span-3"><legend className="mb-2 text-[9px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Eligible products</legend><div className="grid max-h-64 gap-2 overflow-y-auto rounded-3xl border border-border/60 bg-background/50 p-3 sm:grid-cols-2 lg:grid-cols-3">{products.map(product => <label key={product.id} className="flex items-center gap-2 rounded-xl p-2 text-xs hover:bg-muted"><input type="checkbox" name="productIds" value={product.id} defaultChecked={editing?.products.some(item => item.productId === product.id)} /><span className="truncate">{product.name}</span></label>)}</div></fieldset><div className="flex flex-wrap gap-3 lg:col-span-3"><button className="h-12 rounded-full bg-foreground px-5 text-sm font-bold text-background">{editing ? 'Save promotion' : 'Create promotion'}</button>{editing ? <Link href="/admin/promotions" className="h-12 rounded-full px-5 text-sm font-bold leading-[3rem] text-muted-foreground">Cancel editing</Link> : null}</div></form></AdminPanel> : null}
  <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{promotions.map(promotion => <article key={promotion.id} className="overflow-hidden rounded-[2rem] border border-border/60 bg-card/75 shadow-sm"><div className="relative aspect-[16/8] bg-muted">{promotion.bannerMediaAsset ? /* eslint-disable-next-line @next/next/no-img-element */ <img src={promotion.bannerMediaAsset.secureUrl} alt="" className="size-full object-cover" /> : <div className="grid size-full place-items-center"><BadgePercent className="size-8 text-muted-foreground" /></div>}<span className="absolute left-3 top-3 rounded-full bg-black/55 px-2 py-1 text-[8px] font-bold text-white">{promotion.status.replaceAll('_', ' ')}</span></div><div className="p-5"><p className="text-[9px] font-bold uppercase tracking-[0.14em] text-primary/70">{promotion.type.replaceAll('_', ' ')} · {promotion.products.length} products</p><h2 className="mt-2 text-lg font-black">{promotion.title}</h2><p className="mt-1 line-clamp-2 text-xs text-muted-foreground">{promotion.description ?? 'No promotion description yet.'}</p>{promotion.vendorProfile ? <p className="mt-3 text-[9px] font-bold">Vendor: {promotion.vendorProfile.name}</p> : null}{access.permissions.has('promotion:manage') ? <div className="mt-4 flex flex-wrap gap-2"><Link href={`/admin/promotions?edit=${promotion.id}`} className="rounded-full bg-foreground px-3 py-2 text-[9px] font-bold text-background">Edit</Link>{promotion.status !== 'PUBLISHED' ? <Status id={promotion.id} status="PUBLISHED" label={access.permissions.has('approval:review') ? 'Publish' : 'Submit'} /> : <Status id={promotion.id} status="PAUSED" label="Pause" secondary />}<Status id={promotion.id} status="ARCHIVED" label="Archive" secondary /></div> : null}</div></article>)}</section></div></AdminPage>;
}
function Field({ label, children, className = '' }: { label: string; children: React.ReactNode; className?: string }) { return <label className={className}><span className="mb-2 block text-[9px] font-bold uppercase tracking-[0.14em] text-muted-foreground">{label}</span>{children}</label>; }
function Status({ id, status, label, secondary = false }: { id: string; status: string; label: string; secondary?: boolean }) { return <form action={setPromotionStatus}><input type="hidden" name="id" value={id} /><input type="hidden" name="status" value={status} /><button className={secondary ? 'rounded-full border border-border px-3 py-2 text-[9px] font-bold' : 'rounded-full bg-foreground px-3 py-2 text-[9px] font-bold text-background'}>{label}</button></form>; }
