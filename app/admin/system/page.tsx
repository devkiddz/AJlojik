import { Code2, ShieldAlert, Store, Workflow } from 'lucide-react';

import { requireDeveloperAdmin } from '@/features/admin/auth/adminPermissions';
import { AdminMetric, AdminPage, AdminPageHeader, AdminPanel } from '@/features/admin/components';
import { updateWorkspaceCommerceMode } from '@/features/admin/system/actions';
import { prisma } from '@/lib/prisma';

export default async function AdminSystemPage(){const access=await requireDeveloperAdmin();const workspaces=await prisma.workspace.findMany({include:{_count:{select:{vendors:true,products:true,memberships:true}}},orderBy:{createdAt:'asc'}});return <AdminPage><div className="mx-auto max-w-6xl space-y-5"><AdminPageHeader eyebrow="Platform authority" title="Developer System Management" description="Platform capabilities are separate from workspace Super Admin permissions. Only Developer Admin can activate or deactivate multivendor mode."/><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><AdminMetric icon={Code2} label="Developer authority" value="ACTIVE"/><AdminMetric icon={Store} label="Workspaces" value={workspaces.length}/><AdminMetric icon={Workflow} label="Multivendor workspaces" value={workspaces.filter(w=>w.commerceMode==='MULTI_VENDOR').length}/><AdminMetric icon={ShieldAlert} label="Protected capability" value="MULTIVENDOR"/></section><div className="rounded-[2rem] border border-rose-500/25 bg-rose-500/10 p-5 text-sm text-rose-700"><strong>Platform security boundary.</strong><p className="mt-2 text-xs leading-5">The controls below are enforced by Developer Admin server authorization. Workspace Owner and Super Admin roles cannot invoke them.</p></div><AdminPanel title="Workspace commerce modes" description="Disabling multivendor preserves vendor data but blocks onboarding, Vendor Studio and vendor publishing."><div className="grid gap-3 sm:grid-cols-2">{workspaces.map(workspace=><article key={workspace.id} className="rounded-3xl border border-border/60 bg-background/60 p-4"><p className="text-[9px] font-bold uppercase tracking-[.14em] text-primary">{workspace.mode}</p><h2 className="mt-2 text-sm font-black">{workspace.name}</h2><p className="mt-1 text-[10px] text-muted-foreground">{workspace._count.products} products · {workspace._count.vendors} vendors · {workspace._count.memberships} members</p><form action={updateWorkspaceCommerceMode} className="mt-4 flex gap-2"><input type="hidden" name="workspaceId" value={workspace.id}/><select name="commerceMode" defaultValue={workspace.commerceMode} className="h-10 min-w-0 flex-1 rounded-xl border border-border/70 bg-background px-2 text-xs"><option value="SINGLE_VENDOR">Single vendor</option><option value="MULTI_VENDOR">Multivendor</option></select><button className="rounded-full bg-foreground px-4 text-[9px] font-bold text-background">Apply</button></form></article>)}</div></AdminPanel><p className="text-[9px] text-muted-foreground">Authenticated as Developer Admin: {access.actor.email}</p></div></AdminPage>}
